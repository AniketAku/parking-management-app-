{
  "name": "parking-management-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cd web-app && npm run dev",
    "build": "cd web-app && npm run build",
    "preview": "cd web-app && npm run preview",
    "lint": "cd web-app && npm run lint",
    "test": "cd web-app && npm run test",
    "install-deps": "cd web-app && npm install"
  },
  "description": "Parking Management System - Development orchestrator",
  "repository": {
    "type": "git",
    "url": "."
  }
}